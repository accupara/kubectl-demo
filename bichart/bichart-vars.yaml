global:
  namespace: "default"
  appname: bookinventory
  biroot: "/tmp/src/k8s-demo/book_inventory"
  bifrontend: "/tmp/src/k8s-demo/book_inventory/frontend"
  bibackend: "/tmp/src/k8s-demo/book_inventory/backend"
  biDbName: "bi"
  googlecreds:
    mountPath: "/var/secrets/google"
    file: "key.json"
    secret: "google-cloud-key"
    env:
      GOOGLE_APPLICATION_CREDENTIALS: "/var/secrets/google/key.json"
  env:

#yes
frontend:
  enabled: true
  appName: frontend
  serviceName: frontend
  image: node:latest
  port: "5000"
  targetPort: "5000"
  env:


#yes
backend:
  enabled: true
  appName: backend
  serviceName: backend
  image: node:latest
  port: "3000"
  targetPort: "3000"
  env:


#yes
mongodb:
  enabled: true
  appName: mongodb
  serviceName: mongodb
  image: bitnami/mongodb:latest
  port: "27017"
  targetPort: "27017"
  datadir: /root/mongodata
  env:

